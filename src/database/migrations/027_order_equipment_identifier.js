// Универсальный идентификатор объекта заказа (Фаза 10, задача 10.9, решение D5).
//
// Разным нишам нужен один и тот же по смыслу «внешний идентификатор объекта»:
// автосервису — VIN/госномер, кондиционерам — адрес объекта, велосервису —
// серийник рамы, аквариумисту — номер аквариума. Схему заказа по нишам НЕ ветвим
// (это ломает офлайн-модель и синк): добавляем одно опциональное поле, а подпись
// берём из лексикона активного профиля (задача 10.1).
//
// Миграция идемпотентна (`PRAGMA table_info`).

import { addColumnIfMissing } from './add-column.js'

export default {
  id: '027_order_equipment_identifier',

  up: async (db) => {
    await addColumnIfMissing(db, 'orders', 'equipment_identifier', 'TEXT');
  },

  down: async () => {
    // См. 025: SQLite-колонки не удаляются, локальные миграции не откатываются.
  },
};
