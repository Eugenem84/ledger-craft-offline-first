# Интерфейс: дизайн-система Ledger Craft

Документ описывает, как устроен UI после переработки: **какие цвета за что отвечают**,
где живут токены, какие есть общие компоненты и по каким правилам собираются экраны.
Концепция и палитра сохранены: **чёрный фон + жёлтый акцент**, статусная светофорная
логика (ожидание / в работе / готово+оплачено), нижние вкладки, FAB.

> ⚠️ Это только представление. Логика (сторы, репозитории, синк, схема БД) не менялась.

## 1. Роли цвета

| Роль | Токен / цвет | Где применять |
|---|---|---|
| Фон приложения | `--lc-bg` (`#000`) | `body`, `q-page` (`.lc-page`); при картинке-фоне фон страницы становится прозрачным (`.lc-has-bg`) |
| Поверхности | `--lc-surface` / `--lc-surface-2` / `--lc-surface-3` | карточки `.lc-card`, таббар, диалоги; при картинке-фоне — полупрозрачные («стекло», §7) |
| Хром (шапка) | `--lc-chrome` (`#000`) | `q-header` (`.lc-appbar`); при картинке-фоне — `rgba(0,0,0,0.72)` |
| Границы | `--lc-border`, `--lc-border-strong` | разделители карточек и строк |
| Текст | `--lc-text`, `--lc-text-dim` (`.lc-muted`), `--lc-text-mute` (`.lc-mute`) | три уровня важности |
| **Интерактив (жёлтый)** | `$secondary: #ffeb3b`, `--lc-accent` | кнопки, вкладки, ссылки, фокус, FAB |
| **Акцент профиля** | `$primary` + runtime `setCssVar('primary', accent)` | только бейдж/иконка активного профиля |
| Статус: ожидает | `--lc-waiting` (`#ffb300`) | чип, полоса строки и индикатор переключателя |
| Статус: в работе | `--lc-process` (`#e53935`) | чип, полоса строки и индикатор переключателя |
| Статус: готово | `--lc-done` (`#43a047`) | чип, полоса строки и индикатор переключателя |
| Оплачено | `--lc-paid` (зелёный) | отдельный чип рядом со статусом |
| **Опасное действие** | `--lc-danger` = `--lc-process` (красный) | «чип» кнопок удаления/восстановления: `--lc-danger-soft` / `--lc-danger-line` |
| **Подтверждение** | `--lc-ok` = `--lc-done` (зелёный) | кнопки подтверждения, «оплачено» |
| **Внимание** | `--lc-warn` = `--lc-waiting` (янтарный) | индикатор «нет сети» / «не отправлено: N» |
| Синк: всё синхронизировано | `--lc-sync-ok` (`#2e7d32`) | круглый значок синка в шапке. Насыщеннее палитрового `positive` (`#43a047`), который на маленьком значке читался как салатовый (правка владельца 15.09.2026) |

**Палитра — четыре цвета плюс серые** (правка владельца 15.09.2026): жёлтый акцент, красный,
зелёный, янтарный. Роли действий **ссылаются на цвета статусов** (`--lc-danger` = красный
«в работе», `--lc-ok` = зелёный «готово», `--lc-warn` = янтарный «ожидает»), поэтому кнопка
«удалить» и статус «в работе» — один и тот же красный, а пятого оттенка (`deep-orange`) в
приложении нет: он тоже «опасно».

**Ключевое правило.** Жёлтый — это `secondary` и он **не меняется от профиля**.
`primary` — акцент рабочего профиля (пресеты: `#4caf50`, `#ff7043`, `#42a5f5`, `#26c6da`),
он подставляется в рантайме и используется только в бейдже профиля. Так фича «акцент по
специализации» видна, но не перекрашивает половину интерфейса.

Раньше: жёлтый был захардкожен в ~100 местах (`color="yellow"`, `bg-yellow`), а `primary`
переопределялся акцентом профиля → конфликт. Теперь и то и другое разведено по ролям.

## 2. Токены и утилиты

- `src/css/quasar.variables.scss` — палитра Quasar (`$primary`, `$secondary`, `$dark-page`, статусы).
- `src/css/app.scss` — CSS-переменные `--lc-*`, глобальные правки компонентов Quasar и утилиты:

| Класс | Смысл |
|---|---|
| `.lc-page` + `.lc-shell` | фон страницы и её отступы (по бокам — `--lc-pad-x`) |
| `.lc-card` | карточка-контейнер (границы, радиус, вложенные списки без двойных рамок) |
| `.lc-pad` / `.lc-pad-x` | внутренние отступы карточек и панелей (`--lc-pad-card`) |
| `.lc-eyebrow` | надзаголовок капсом (секции, лейблы) |
| `.lc-muted` / `.lc-mute` | вторичный / третичный текст |
| `.lc-money` | цифры (tabular-nums) |
| `.lc-status--waiting/process/done/paid/unknown` | чипы статусов: насыщенный цвет + мягкая заливка и тонкая рамка из парных токенов `--lc-*-soft` / `--lc-*-line` (их же берёт индикатор переключателя — см. ниже) |
| `.lc-order-row--waiting/process/done` | полоса статуса слева в списке заказов |
| `.lc-toggle` + `.lc-toggle::before` + `.lc-toggle--waiting/process/done/paid` | сегментированные переключатели статуса/оплаты в карточке заказа: активный сегмент оформлен **как чип статуса** — та же пара токенов (`--lc-*-soft` / `--lc-*-line`) и цвет подписи с иконкой, а индикатор лежит под подписями и «переезжает» к активному сегменту (`--lc-toggle-count` — число сегментов, `--lc-toggle-shift` — позиция). Иконки — общая карта `ORDER_STATUS_ICONS`. Правка владельца 15.09.2026 |
| `.lc-linerow`, `.lc-col-name/num/qty/del` | строки позиций (работы/материалы/товары). В строках показываются название, цена, **кол-во** и сумма: себестоимость из карточки заказа убрана (14.21), количество — целое ≥ 1 (14.19) и меняется шагомером «‹ N ›» (правка владельца 15.09.2026: `.lc-col-qty--stepper` — колонка под шагомер) |
| `.lc-stepper`, `.lc-stepper__btn`, `.lc-stepper__value` | шагомер количества: явные стрелки влево/вправо вокруг значения (правка владельца 15.09.2026) |
| `.lc-kpi`, `.lc-kpi__label`, `.lc-kpi__value` | плитки и цифры аналитики |
| `.lc-hide-sm` | прячет второстепенные колонки на экранах ≤ 420px |
| `.lc-fab` | позиция плавающей кнопки над таббаром (с учётом safe-area; не липнет к кромке — `right: max(…, 12px)`) |
| `.lc-seg` | сегментированный переключатель вне карточки заказа (склад, «Аналитика»): «таблетка» на `--lc-surface-3`, активный сегмент — мягкая жёлтая заливка с кольцом (правка владельца 15.09.2026) |
| `.lc-order-list` / `.lc-order-row` | список заказов: заказ — отдельная карточка (рамка, радиус, отступ 10px), полоса статуса — «пилюля» слева (правка владельца 15.09.2026: строки сливались в одно полотно) |
| `.lc-viewport` | контейнер страниц (`q-page-container`): срезает по горизонтали вылет входящей страницы при переходе между разделами (`overflow-x: clip` — не создаёт скролл-контейнер) |
| `.lc-has-bg` | «стеклянный» режим каркаса: вешается на `q-layout` **только когда в сборке есть картинка-фон** (см. §7). Делает `--lc-bg` прозрачным (страница пропускает картинку), а `--lc-surface*` и `--lc-chrome` — полупрозрачными: `rgba(18,18,18,0.86)` на чёрном даёт почти прежний `#121212` |
| `.lc-swipe-next-*` / `.lc-swipe-prev-*` | переход между разделами: уходящая страница гаснет (0.12s), входящая приезжает со стороны движения (0.22s). Имя перехода считает `utils/tabSwipe.js` |

Inline-стили в разметке не используются: цвет/размер берётся из токена или класса.

**Кнопки — один «чип»-язык** (правка владельца 15.09.2026). Кнопка, чип статуса и сегмент
тумблера — один визуальный элемент: «пилюля» (`border-radius: 999px`) с мягкой заливкой и
тонким кольцом. Иерархия держится на свечении, а не на сплошной заливке:

- **главное действие** (`color="secondary"`) — заливка `--lc-accent-soft` (12 %), кольцо
  `--lc-accent-border` (35 %) и мягкое жёлтое свечение: «новый заказ» (FAB), «новая работа»,
  «добавить материал», «Сохранить», кнопки обновления;
- **второстепенное** (`outline`) — тот же «чип» без свечения;
- **тихое** — `flat`: прозрачный фон, цветной значок и подпись (отклик на нажатие даёт Quasar);
- **опасное / подтверждающее** — те же «чипы» на красном (`--lc-danger`) и зелёном (`--lc-ok`).

Палитровые классы Quasar (`bg-secondary`, `text-negative`, `bg-deep-orange`…) переопределены
**один раз** в `app.scss`, поэтому в разметке цвет — это роль («опасно», «подтвердить»), а не
оттенок. Исключение — индикатор синка в шапке (`.lc-sync-chip--icon`): это знак состояния, а не
кнопка, он остаётся сплошным цветным.

**Поля по краям экрана.** Боковое поле страницы — токен `--lc-pad-x`, внутреннее поле
карточек/строк — `--lc-pad-card`. Правка владельца 15.09.2026 («приложение слишком узкое»):
на телефоне боковое поле страницы — `0` (карточки во всю ширину экрана), внутреннее поле
карточки — `12px` (≤360px — `10px`); с `600px` оба возвращаются к `16px`. Раньше и страница,
и карточка добавляли по 16px, и контент уезжал от края на 32px — на узком экране блоки
выглядели «подвешенными». Внутренние панели вместо `q-pa-md`/`q-px-md` берут `.lc-pad`/`.lc-pad-x`.

## 3. Общие компоненты (`src/components/ui/`)

| Компонент | Зачем |
|---|---|
| `LcPageHeader.vue` | заголовок экрана (eyebrow + подпись + слот действий) |
| `LcSectionCard.vue` | секция-карточка (заголовок + содержимое) |
| `LcStatusChip.vue` | статус заказа: иконка + слово + цвет (слова из `ORDER_STATUSES`) |
| `LcEmptyState.vue` | пустое состояние: иконка, что произошло, что делать |
| `LcFab.vue` | кнопка основного действия на экране |
| `LcQuantityStepper.vue` | количество «‹ N ›»: явные стрелки влево/вправо (меньше/больше), целое ≥ 1; заменяет числовое поле с системными стрелками «вверх/вниз» |
| `LcDialogShell.vue` | оболочка диалога: заголовок, тело, «Отмена» + основное действие. Высота ограничена вьюпортом (`max-height: calc(100dvh - 32px)`), прокручивается только тело — длинный список внутри диалога не вылезает за экран (правка владельца 15.09.2026) |
| `AuthShell.vue` | каркас экранов `/login` и `/register` (бренд, QLayout + QPage) |

Правило: новый экран берёт готовые компоненты, а не рисует свои кнопки/секции/диалоги.

## 4. Каркас приложения

> ⚠️ **Ловушка Quasar:** `q-tab-panel` должен быть **прямым ребёнком** `q-tab-panels`.
> Quasar собирает список панелей из vnode'ов слота и сверяет их проп `name`
> (`quasar/src/composables/private.use-panel/use-panel.js` → `updatePanelsList`),
> а рендерит ровно одну панель (`getPanelContent()`). Если обернуть панель в свой
> компонент без проброса `name`, список панелей пуст и **содержимое всех вкладок
> не отрисовывается** — вкладки видны, а под ними пусто.
> Поэтому в карточке заказа `q-tab-panel` объявлены в `pages/OrderDetailsPage.vue`,
> а `OrderOverviewPanel` / `OrderServicesPanel` / `OrderMaterialsPanel` — это содержимое
> с обычным корнем `<div>`. Тест `test/order-tabs.test.js` следит за этим.

> ⚠️ **Вторая ловушка — корень страницы.** Страницы разделов анимируются переходом
> (`<Transition>` в `MainLayout.vue`), а он умеет ровно **один** корневой элемент. Если у
> страницы несколько корней (`q-page` + диалоги рядом — так было в `CatalogPage.vue`), это
> фрагмент: у него нет своего элемента, а при уходе со страницы Vue вешает leave-классы на
> якорный текстовый узел фрагмента (`el.classList` у текстового узла нет) — исключение в
> рендерере убивает приложение, экран чернеет и касания не работают (живой прогон
> 17.09.2026: «перелистываю на каталог — обратно уже чёрный экран и всё ни туда ни сюда»).
> Правило: **диалоги и любые блоки живут внутри `<q-page>`** (`StorePage`, `OthersPage` так и
> сделаны). Дополнительно переход в каркасе анимирует обёртку `div.lc-view`, а не страницу,
> поэтому фрагмент-корень больше не может уронить приложение. Следят
> `test/swipe-navigation.test.js` (идёт по `routes.js` и требует один корень у каждой
> страницы каркаса) и `test/order-tabs.test.js`.

- `src/layouts/MainLayout.vue`: шапка с переключателем профиля, нижняя навигация.
  Состав вкладок — массив `tabs` (`v-for` + `class="col"`), видимость — флаги пресета
  (`useFeatures()`), подписи — лексикон (`useLexicon()`). При скрытии разделов сетка не «разъезжается».
- Таббар: `class="lc-tabbar"`, `padding-bottom: env(safe-area-inset-bottom)` для Android.
- **Свайп по разделам** (правка владельца 17.09.2026). Разделы листаются горизонтальным
  жестом — как карусель, но без «тянется за пальцем»: жест распознаёт Quasar
  (`v-touch-swipe:0.25:24.horizontal` на `q-page-container` в `MainLayout.vue`), решение
  принимает чистая логика `src/utils/tabSwipe.js`, переход — короткий CSS-переход
  (`.lc-swipe-next` / `.lc-swipe-prev`, 0.12s уходящая + 0.22s входящая).
  Что важно знать, трогая каркас:
  - **порядок свайпа — это порядок таббара** (`tabs` в `MainLayout.vue`), а не порядок
    маршрутов в `router/routes.js` («каталог» там объявлен последним);
  - `v-touch-swipe` зовёт обработчик **на каждом движении пальца** — иначе один жест
    увёл бы на несколько разделов; поэтому «один свайп = один переход» считается своим
    касанием (`@touchstart` → `onSwipeStart`, `@touchend`), и там же запоминается
    координата начала: жесты из первых/последних 24px отданы системе (Android 15 —
    «назад» от левого края), а касание внутри открытого `q-dialog`/`q-menu` свайп не
    начинает (иначе раздел менялся бы под диалогом, а Quasar закрывает диалог при смене
    маршрута — заполненная форма потерялась бы);
  - пороги строже дефолта Quasar: на длинном списке дефолт принимал за свайп
    диагональную прокрутку (`absY < 100` — это много), поэтому скорость поднята до
    0.25px/мс и добавлена своя проверка ухода по вертикали (`SWIPE_MAX_DRIFT_Y`);
  - переход — `mode="out-in"` (страницы не накладываются: наложение требует
    `position: absolute` у уходящей, и контейнер на кадр меняет высоту — список «прыгает»);
    слайд только у входящей страницы, уходящая просто гаснет;
  - `transform` на странице не только двигает её, но и становится «содержащим блоком» для
    `position: fixed`: из-за этого `LcFab.vue` рендерится через `Teleport to="body"`
    (иначе на длинной странице кнопка прилипала бы к низу **контента**), а горизонтальный
    вылет входящей страницы срезает `.lc-viewport { overflow-x: clip }` (`clip`, не
    `hidden`: не создаёт скролл-контейнер).
- **Фоновая картинка специализации** (та же правка). Под контентом лежит слой
  `src/components/ui/LcAppBackground.vue` (`position: fixed`, `z-index: -1`,
  `pointer-events: none`): одна картинка на специализацию (`preset_key`), кроссфейд при
  смене профиля и движение при листании. Элемент картинки **шире экрана** (размер и позиция —
  явными px из `backgroundLayout()`), поэтому сдвиг показывает продолжение рисунка, а не
  пустоту; окно обзора стоит на центре контента рисунка и ходит внутри него. Файлы —
  `src/assets/backgrounds/<preset_key>.webp` (ТЗ и имена — в §7 и в README рядом с папкой).
  **Пока файла нет, внешний вид приложения не меняется:** класс `.lc-has-bg` вешается только
  при наличии картинки, и именно он делает поверхности и хром полупрозрачными (см. §1/§2).
- **Системные панели Android.** Отступ под статус-бар задаёт Capacitor, а не CSS: в
  `src-capacitor/capacitor.config.json` стоит `android.adjustMarginsForEdgeToEdge: "auto"`
  (Android 15, `targetSdk 35` включает edge-to-edge принудительно; в Capacitor 8 это дефолт).
  Тема `AppTheme.NoActionBar` (`src-capacitor/android/app/src/main/res/values/styles.xml`,
  её ставит `BridgeActivity`) делает фон окна, статус-бар и навигационную панель чёрными,
  иконки — светлыми. `env(safe-area-inset-top)` на Android для статус-бара пуст, поэтому
  верстать отступы на него нельзя.
- `SyncStatusBar.vue` — индикатор сети и синка: живёт **только** в шапке `MainLayout.vue`
  (единственное место монтирования, задача 14.11). С правки владельца 15.09.2026 это **только
  значок** без подписи (`.lc-sync-chip--icon`, круглый чип 24px — состояние читается по цвету и
  иконке, текст живёт в тултипе и `aria-label`), а рядом показана очень мелкая серая версия
  приложения (`.lc-app-version`). Версию собирает `useUpdateStore.currentVersionShort`: сначала
  нативная версия устройства, а в веб-сборке (нативной части там нет) — вшитая в бандл из
  `src-capacitor/android/gradle.properties` (`build.env` в `quasar.config.js`); если версия
  неизвестна совсем — строка не рисуется.
- `OrderDetailsPage.vue` объявляет собственный `QLayout` + `QPage` (маршрут вне `MainLayout`);
  нижнего таббара у него нет, поэтому `LcFab` там не используется — создание позиции живёт
  в панелях вкладок (`OrderServicesPanel` / `OrderMaterialsPanel`).

## 5. Правила текста и доступности

- Подписи — строчными, полными словами: «ожидает», «в работе», «готово», «оплачено»
  (раньше — обрезки «ожид», «враб», «сохр», «опл»). В кнопках капс убран глобально
  (`text-transform: none` в `app.scss`) — до этого подписи с `no-caps` и без него выглядели
  по-разному.
- Статус и оплата дублируются словом и цветом (не только цветом).
- Размер тап-зоны ≥ 40px (`.q-item` в `.lc-card` — ~52px), у иконочных кнопок — `round dense` + подсказка.
  Исключение — сегменты статуса/оплаты в шапке заказа: по правке живого прогона ряд сделан
  компактным (`dense` + `min-height: 28px`), чтобы шапка занимала меньше места. Иконки сегментов
  (14px, как у чипа) на экранах уже 360px скрываются — иначе ряд из трёх статусов и «оплачено»
  не помещается в карточку.
- Деструктивные действия: красный/оранжевый цвет, подтверждение через `DeleteConfirmPage`.
  Полный сброс и удаление локальной БД (Фаза 12, 12.4/12.5) в пользовательских настройках
  **не показываются**, пока не включён тумблер «разработка»: тогда появляется панель
  «Режим разработчика» (`DeveloperPanel.vue`, вкладки «логи/диагностика/очередь/опасное»).
  Тумблер работает и в боевой сборке (флаг в `localStorage`), панель грузится лениво.
- Диалоги адаптивные (`max-width: min(92vw, 520px)`), вместо `min-width: 400px`,
  который вылезал за экран телефона. Высоту диалога ограничивает вьюпорт
  (`max-height: calc(100dvh - 32px)` в `LcDialogShell`), прокручивается только тело —
  иначе карточка с длинным списком (например, история склада товара) уезжала за пределы
  экрана, и до кнопок нельзя было добраться.
- Вкладки внутри раздела — `q-tabs` + `q-tab-panels`, панели **прямые дети**
  `q-tab-panels` (иначе Quasar не отрисует содержимое — ловушка в §4). Page-level
  действие (FAB) показывается только на «своей» вкладке (`v-if="tab === 'goods'"`).
- **Уведомления Quasar — внизу** (`position: 'bottom'`, правка владельца 17.09.2026).
  В карточке заказа позиция задана явно (один хелпер `notify` в `OrderDetailsPage.vue`):
  уведомление о копировании отчёта «выскакивало» сверху и перекрывало шапку с номером
  и статусом заказа. Новые экраны тоже предпочитают низ — там уведомление не спорит с
  заголовком и действиями страницы.
- **Отчёт клиенту** (правка владельца 17.09.2026): кнопка «поделиться» в карточке
  заказа копирует либо публичную ссылку (её выдаёт сервер, задача 9.4), либо **минимальный
  текст отчёта** — статус, позиции с ценами, итог, комментарий и подпись «Отчёт
  сформирован в ledgerCraft.ru». Номера заказа, клиента, объекта и даты нет (клиент знает их
  сам, а время сообщения показывает мессенджер), слова «оплачено» тоже нет — про оплату мастер
  скажет словами, а текст должен умещаться даже в одно SMS. Формат выбирается переключателем
  «отчёты клиентам» в «Ещё»
  (`src/utils/reportSettings.js`, настройка устройства, не профиля); текст собирается на
  устройстве (`src/utils/reportText.js`), поэтому работает офлайн.

## 6. Что проверять при изменениях UI

1. `npm run lint` и `npm test` — тест `test/pages-layout.test.js` следит за тем, чтобы
   `QPage` вне `MainLayout` был обёрнут в `QLayout` (иначе Quasar не монтирует страницу).
2. Проход по маршрутам: `/orders`, `/orders/new`, `/orders/:id`, `/store`, `/catalog`,
   `/analytic`, `/other`, `/login`, `/register`.
3. Смена профиля (bike/auto/hvac/aquarium): акцент бейджа, подписи лексикона, состав вкладок.
   Добавление профиля — только выбор из списка ниш; переименования/смены пресета у готового нет.
4. Узкий вьюпорт (360px): строки позиций, список заказов (карточки), диалоги, шапка, ряд
   сегментов статуса (там скрываются иконки), период в «Аналитике» (строка переносится).
5. Индикатор синка (компактный чип в шапке): «требуется вход» / «нет сети» (янтарный, не
   красный: офлайн — не сбой) / «не отправлено: N». Это единственный сплошной цветной чип —
   остальные кнопки-«чипы» мягкие.
6. Карточка заказа: статус и оплата — одним компактным рядом переключателей (`dense`,
   `min-height: 28px`), «оплачено» — таким же тумблером, без дублирующих чипов над ними;
   активный сегмент выглядит как чип статуса (мягкая заливка + тонкая рамка + иконка, не
   сплошная палитровая заливка), создание позиции — ровно одной кнопкой (в панели вкладки,
   FAB на странице нет).
7. Кнопки: сплошных заливок нет — цвет читается как роль (жёлтая — главное действие, красная —
   опасное, зелёная — подтверждение), форма — «пилюля», у главного действия мягкое свечение;
   проверять и на основных экранах, и в диалогах (там подтверждение стоит рядом с тихой «Отменой»).
8. Android (живое устройство): контент не уезжает под системные панели — отступы даёт
   Capacitor (`adjustMarginsForEdgeToEdge: "auto"`), полосы под статус-баром и навигацией чёрные.
9. Режим разработчика: тумблер «разработка» в «Ещё» показывает панель с вкладками
   «логи/диагностика/очередь/опасное» (работает и в prod-сборке — флаг в `localStorage`,
   панель — ленивый чанк); при выключенном тумблере секции нет.
10. Отчёты клиентам: переключатель «ссылка на отчёт / текст отчёта» в «Ещё» меняет то, что
    копирует кнопка «поделиться» в карточке заказа (текст собирается на устройстве и
    заканчивается подписью `ledgerCraft.ru`); уведомление о копировании — внизу экрана.
11. Свайп по разделам (на телефоне или в DevTools с эмуляцией касаний + перезагрузка
    страницы: Quasar определяет поддержку touch один раз при загрузке): листание слева
    направо и обратно **по всем разделам, включая каталог** (дефект 17.09.2026 был именно на
    нём — страница с диалогами вне `q-page` роняла приложение при уходе), на первой/последней
    вкладке ничего не происходит, свайп от самого
    края экрана раздел не меняет, открытый диалог (или меню профиля) свайпом не
    закрывается, прокрутка длинного списка раздел не переключает. Тап по таббару даёт ту
    же анимацию, что свайп.
12. Фоновая картинка (когда файлы добавлены): заполняет экран **целиком** и едет **по
    горизонтали** при листании (вкладки в сторону — фон за ними, но меньше), видна между
    карточками и сквозь них, шапка и таббар — «стекло», текст в карточках и жёлтый акцент
    читаются; на первой/последней вкладке край кадра не оголяется; при смене профиля
    картинка меняется кроссфейдом, а без файла фон остаётся чёрным и поверхности сплошными.

## 7. Фоновая картинка специализации

Правка владельца 17.09.2026: «у приложения будет фоновая картинка, у каждой специализации
своя, и при свайпе она двигается».

**Как это работает.** Картинка активного профиля лежит под интерфейсом
(`src/components/ui/LcAppBackground.vue`: `position: fixed`, `z-index: -1`,
`pointer-events: none` — касания она не перехватывает). При смене профиля картинка
меняется кроссфейдом (0.32s), при листании разделов — едет.

**Главное правило — картинка заполняет экран целиком и едет по горизонтали** (правка владельца
17.09.2026). Как это устроено:

- элемент картинки **шире экрана** — размер и позиция задаются явными px из `backgroundLayout()`
  (`src/utils/tabSwipe.js`), края скрыты за вьюпортом (`.lc-appbg` → `overflow: hidden`), поэтому
  сдвиг показывает **продолжение рисунка**. Ловушка, из-за которой это пришлось переделать: у
  `<img>` с `object-fit: cover` содержимое обрезается по границам элемента, и «запас» существует
  только если элемент шире вьюпорта — иначе сдвиг обнажал чёрный фон («картинка срезана по
  краям, а за краями должно быть продолжение», живой прогон 17.09.2026);
- окно обзора стоит на **центре контента рисунка**, а не кадра: у картинок бывают пустые поля
  (у `bike.webp` контент занимает 10–70 % ширины), и центрирование по кадру показывало пустоту
  сбоку. Границы контента считает `contentBounds()` по колонкам яркости (маленький canvas 48×48);
- ход ограничен контентом рисунка (`BACKGROUND_CONTENT_INSET`), сверху — `BACKGROUND_SHIFT_MAX`
  (25 % ширины экрана). У `bike.webp` при экране 360 px это ±51 px: окно обзора гуляет
  89…551 px внутри контента 80…560 px — пустые поля не попадают в кадр ни на одной вкладке;
- если своего запаса у рисунка нет (кадр ровно в пропорциях экрана), картинка получает
  минимальное увеличение — ровно чтобы ход появился без оголённого края;
- вьюпорт меряется в DOM (`getBoundingClientRect` слоя), а не берётся из `$q.screen`; на
  `resize`/поворот экрана раскладка пересчитывается.

Файлы лежат в `src/assets/backgrounds/` и подхватываются сборщиком
(`src/services/backgroundAssets.js`, `import.meta.glob`): **имя файла = `preset_key`** из
`src/domain/presets/*.js` — новая ниша добавляется файлом пресета и файлом картинки, без
правок кода экранов. Пока файла нет, фон чёрный, а поверхности сплошные (`.lc-has-bg`, §2) —
ниша без картинки выглядит как раньше.

### ТЗ на картинку (одна на специализацию)

| Параметр | Значение | Почему так |
|---|---|---|
| Файл | `src/assets/backgrounds/<preset_key>.webp` — `bike` (есть), `auto`, `hvac`, `aquarium` (и необязательный `default` для профиля без пресета) | ключ = `preset_key`; перечень — в README рядом с папкой |
| Формат | WebP lossy, quality 88–90 | на 30–50 % легче JPEG; поддерживают все целевые WebView (`minSdk 23`). PNG/JPEG тоже заработают — сконвертируем |
| Соотношение сторон | **квадрат** — лучший вариант: запас для хода максимальный (по 28 % ширины с каждой стороны уходит за кадр в покое); портрет (9:20) тоже работает, но запаса нет — картинка получит увеличение ×1.2, и ход будет меньше | ход берётся из «лишней» ширины кадра, а на портретном экране её даёт именно широкий кадр |
| Размер | 1440 × 1440 или 2048 × 2048 — с запасом на увеличение и ход; 1024 × 1024 достаточно, если картинка мягкая и тёмная | кадр растягивается на высоту экрана (на 1080p это ×~2.3 для квадрата), поэтому мелкий кадр может мылиться |
| Вес | ≤ 300 КБ на файл, все четыре ≤ 1.2 МБ | картинки попадают в OTA-бандл (сейчас ≈1.2 МБ): +1.2 МБ удваивает размер сборки. Пример: `bike.webp` — 17 КБ |
| Ориентация | портретная композиция; отдельная ландшафтная не нужна | в ландшафте видна горизонтальная полоса кадра |
| Цвет | sRGB, 8 бит/канал, без CMYK и «широких» профилей | иначе цвета уедут в WebView |
| Прозрачность | не нужна; края лучше увести в чистый `#000` | фон приложения чёрный — фейд сольётся с шапкой и таббаром |
| Яркость | средняя ≤ 20 %, максимум в центре ≤ 40 % | карточки полупрозрачные (`rgba(18,18,18,0.86)`): иначе текст `#f4f4f4` и жёлтый акцент теряют контраст. Пример: `bike.webp` — средняя ~1 %, максимум 25 % |
| Композиция | Контент рисунка держите **в пределах кадра, но доходящим до его краёв**: окно обзора стоит на центре контента и ходит внутри него. Пустые поля по краям рисунка в кадр не попадают, но они и не дают хода — у `bike.webp` контент занимает 10–70 % ширины, поэтому ход ±51 px; если контент доходит до краёв, ход будет максимальным (`BACKGROUND_SHIFT_MAX` = 25 % ширины экрана). По высоте: сверху ≈7 % закрыто шапкой, снизу ≈10 % — таббаром | «дальний план» на то и дальний, что края рисунка уходят за экран; но пустоту в кадр пускать нельзя |
| Контент | тёмные мягкие тона (боке, градиенты); **без текста, логотипов, рамок и «интерфейсных» элементов** | всё перечисленное будет спорить с интерфейсом |
| Градиенты | добавить лёгкий шум 0.5–1 % | на OLED широкие тёмные переходы дают полосы (бандинг) |
| Акцент | под специализацию: `auto` — `#ff7043`, `bike` — `#4caf50`, `hvac` — `#42a5f5`, `aquarium` — `#26c6da`. **Жёлтый `#ffeb3b` в картинке не использовать** | жёлтый — интерактив (кнопки, активная вкладка); статусные красный/зелёный/янтарный тоже не заливать плоскостями |
| DPI-варианты | не нужны (один файл, без `@2x`/`@3x`) | это веб-слой: `res/drawable-*dpi` здесь не работает |

Ход параллакса выводится из запаса кадра (`backgroundLayout()`), поэтому «сильнее двигать» =
«взять кадр шире пропорций экрана»: квадрат даёт максимальный ход (±24 % ширины экрана), кадр
ровно в пропорциях экрана — минимальный (±10 %, за счёт увеличения ×1.2). Вес файла от пропорций
почти не зависит, так что квадрат выгоден: движение получается бесплатно.

### Как добавить

1. Положить файл в `src/assets/backgrounds/` с именем `preset_key` активного профиля.
2. Пересобрать (или выкатить OTA-бандлом) — картинка выбирается по активному профилю сама,
   правок в коде не нужно.
3. Проверить по пунктам 11–12 из §6.

