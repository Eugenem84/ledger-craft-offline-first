# Архитектура Ledger Craft

> Offline-first система учёта для мастерских: приём заказов (ордеров), клиенты, каталог
> работ и услуг, склад запчастей, модели техники, аналитика. Приложение обязано работать
> без интернета, а при появлении сети — синхронизироваться с сервером.

## 1. Общая схема слоёв

```
┌─────────────────────── UI (Quasar / Vue 3) ───────────────────────┐
│  Pages (OrdersPage, OrderDetailsPage, StorePage, CatalogPage,     │
│         AnalyticPage, OthersPage)                                  │
│         │  читают/пишут состояние                                  │
│         ▼                                                          │
│  Pinia stores (useOrdersStore, useClientsStore, ...)               │
│         │  вызывают                                                │
│         ▼                                                          │
│  Repositories (*Repo)  ─── операции ──► operationsRepo             │
│         │                                 (очередь INSERT/UPDATE/  │
│         ▼                                     DELETE для сервера)  │
│  Queries (готовые SQL-строки)                                     │
│         │                                                          │
│         ▼                                                          │
│  Adapter БД (выбор по платформе, задача 4.3)                        │
│   ├─ db.js — единая точка доступа (делегат на активный адаптер)      │
│   ├─ sqljs-web-adapter.js       (браузер: sql.js + localStorage)     │
│   └─ sqlite-capacitor-adapter.js (Android: нативный SQLite, файл)    │
│         ▼                                                          │
│  Локальная БД: файл SQLite на устройстве / дамп sql.js в браузере    │
└──────────────────────┬─────────────────────────────────────────────┘
                       │  SyncService (services/syncService.js)
                       │    local→server: очередь operations → POST /api/sync
                       │    server→local: GET /api/sync-updates?table=&since=
                       ▼
               Axios (services/api.js, заголовок X-Sync-ID)
                       │
                       ▼
        Laravel API на VPS: dev / prod (см. §1.1 «Среды»)
```

### 1.1. Среды: dev-VPS и prod-VPS (с 12.09.2026)

| Контур | Адрес API | Назначение |
|---|---|---|
| **dev-VPS** | `https://dev.medovf2h.beget.tech/api` | песочница: обкатка новых фич, БД можно снести и накатить заново |
| **prod-VPS** | `https://<prod-домен>/api` (домен уточняется — TODO 11.1) | боевой контур с реальными данными мастерских |

Код один и тот же (`main` + `master`), контур выбирается адресом API: у клиента — `VITE_API_URL`
(env-файл, задача 7.1), у сервера — `APP_URL` и домен в Traefik. Порядок выката и команды:
`README.md` §«Среды и выкат» (клиент) и **`LedgerCraftDocker03/docs/ENVIRONMENTS.md`** — канонический
документ по средам (домены, CORS, SSH, команды выката, smoke, грабли, чек-лист prod); там же
чек-лист живого прогона — `TODO.md`, Фаза 11.

## 2. Ключевая идея: локальный `id` и серверный `server_id`

Каждая запись имеет два идентификатора:

- **`id`** — локальный, `TEXT`, UUID v4. Генерируется на устройстве мгновенно. Именно это
  позволяет создавать записи полностью офлайн (без ожидания сервера).
- **`server_id`** — серверный, `BIGINT/INTEGER`. Присваивается только после того, как
  запись уехала на сервер (на сервере это автоинкремент).

Ссылки на другие записи (внешние ключи) тоже хранятся в паре:
`xxx_id` (локальный UUID родителя) + `xxx_server_id` (серверный id родителя).

**Правило согласованности:**
- внутри устройства всё связано по локальным `id` (UUID);
- на сервер уходят только серверные id (`server_id`);
- если у родителя ещё нет `server_id`, операция, ссылающаяся на него, **не может быть
  отправлена** — она откладывается точечно и дожимается в следующей «волне» того же `sync()`
  (задача 3.2).

Именно эта «связность через server_id» порождает почти все баги синхронизации в истории
проекта (коммиты про «нет server_id у order_service» и т.п.).

## 3. Порядок запуска приложения (boot)

Ключевой boot-файл — `src/boot/db.js` (Quasar запускает его до рендера приложения):

1. `dbAdapter.init()` — открывает БД (веб: sql.js + localStorage; Android: файл SQLite).
2. Создаёт служебную таблицу `migrations` и применяет миграции из
   `src/database/migrations/index.js` (каждая — один раз, по `id`), сверяет версию схемы.
3. **Сеть здесь не трогаем** (задача 6.1): раньше `await syncService.sync()` блокировал рендер,
   пока идёт обмен с сервером. Теперь синк стартует из `src/App.vue` в `onMounted()` —
   `syncService.startAutoSync()` запускает первый проход фоном (следующим тиком таймера),
   то есть приложение рисуется, не дожидаясь сети.
4. Остальные boot-файлы: `axios.js` (дефолтный axios Quasar) и `pinia.js`.

## 4. Синхронизация (подробно)

### 4.1. Направление local → server

1. Любое изменение пишется **сразу** в локальную БД и в очередь `operations`
   (`id`, `type`: insert/update/delete, `table`, `payload` JSON, `status`, `attempts`,
   `created_at`, `updated_at`). Статус операции: `pending` (ждёт отправки) → `sending`
   (улетела, ответа ещё нет) → `synced` (сервер подтвердил, идёт «примирение» локальной
   записи); `failed` — «сдалась» (см. п. 3).
2. `_syncLocalToServer()` (в `syncService.js`) сначала возвращает в работу «зависшие»
   in-flight операции (`operationsRepo.recoverInFlight()`: `sending` → `pending`,
   `synced`+insert → `pending`, `synced`+update/delete → удаление), затем работает
   «волнами» — не больше `MAX_SYNC_WAVES = 5` за один `sync()`:
   - выгружает очередь через `operationsRepo.dequeue()` — только `pending` (операции, по
     которым сервер уже ответил в этом прогоне, повторно не берутся);
   - парсит `payload` и готовит внешние ключи (`_prepareForeignKeys`):
     - «сигнальное» поле вида `xxx_server_id` → значение подставляется в `xxx_id`,
       сигнальное поле удаляется из payload;
     - локальный `xxx_id` → ищется `server_id` в таблице-родителе;
   - строит граф зависимостей (`fkTransformationMap` + локальные id родительских операций
     батча) и сортирует операции топологически (`_sortByDependencies`, алгоритм Кана):
     родители уезжают раньше детей; тай-брейк — `TABLE_ORDER`, затем `created_at`;
   - операция с **неготовым** FK откладывается точечно (остальной батч уезжает);
   - помечает батч `sending` (`operationsRepo.markSending`) и отправляет одним
     `POST /api/sync` — «отправлено» фиксируется **до** сетевого вызова.
3. Обработка ответа `{ synced: [...], errors: [...] }` (`_findSyncResult` → `_sendOperations`):
   - insert → `operationsRepo.markSynced` в одной транзакции удаляет операцию и проставляет
     локальной записи `server_id` из ответа;
   - ошибка по операции → `operationsRepo.registerFailure`: неисправимая ошибка
     (`RECORD_NOT_FOUND`, `FORBIDDEN_NOT_OWNER`, `MISSING_ID_FOR_UPDATE`/`_DELETE`, битый
     payload) или исчерпанный лимит (`MAX_OPERATION_ATTEMPTS = 5`) переводят операцию в
     `failed` — «сдалась», она больше не отправляется; иначе операция возвращается в `pending`
     с увеличенным `attempts`. Раньше любая ошибка вечно возвращала её в `pending`: очередь
     тихо копила дубли, а неисправимая операция висела навсегда (дефект живого прогона 11.6).
     `failed`-операции видны в индикаторе синка и убираются вручную
     (`syncService.discardFailedOperations()` из «Режима разработчика»);
   - ошибка сети (исключение из `api.send`) → `markPending` для всего батча;
   - 200 OK **без ответа по операции** → операция возвращается в `pending` и повторится следующим
     `sync()`: доставленной считается только та, по которой сервер ответил явно (задача 3.5).
     Сервер подтверждает каждую операцию (в том числе «пустой» `update`/`delete`), поэтому
     «молчание» в ответе — это аномалия, а не «уже применено».
4. Волна повторяется, пока есть прогресс (сервер подтвердил хотя бы одну операцию) и остались
   готовые операции: ребёнок, чей родитель получил `server_id` в предыдущей волне, уезжает
   **в том же** `sync()`. Прогон прекращается, когда очередь пуста, готовых операций нет или
   сервер не подтвердил ни одной.
5. Сбой приложения между отправкой и ответом операцию **не теряет**: она осталась в очереди
   (`sending`/`synced`), а следующий `sync()` возвращает её в работу. Повторная отправка дублей
   не создаёт: на сервере идемпотентность по `uuid_id`, у `order_service` — по
   `order_id + service_id` (задача 3.5).

> ✅ Двойной вызов `_syncLocalToServer()` (костыль под двухфазную отправку «родитель → ребёнок»)
> убран в задаче 3.1, детерминированный порядок и «дожим» отложенных операций сделаны в
> задаче 3.2 (граф FK + топосортировка + волны), а статусы очереди и восстановление после
> сбоя — в задаче 3.3. Проверено рантайм-сценариями в Node на настоящем SQLite: заказ офлайн
> (категория/клиент → услуга → заказ → связь «заказ-услуга») уезжает за один `sync()`, очередь
> пуста; битый FK откладывается точечно; сбой «после отправки, до ответа» и недоступная сеть
> не теряют операцию, а возвращают её в работу.

### 4.2. Направление server → local

`_syncServerToLocal()` (в `syncService.js`):

1. Для каждой таблицы из `this.repos` берёт **свой** курсор выдачи
   (`meta.last_synced_at:<table>`; если своего ещё нет — старый общий `last_synced_at`)
   и делает `GET /api/sync-updates?table=<table>&since=<cursor>`.
2. Для каждой полученной записи вызывает `repo.applyServerRecord(record)`:
   - ищет локальную запись по `server_id`;
   - не найдена → вставляет (с новым локальным UUID);
   - найдена → сравнивает `updated_at` и обновляет, если серверная версия новее;
   - серверные FK переводятся обратно в локальные UUID через таблицы-родители.
3. Курсор таблицы сдвигается **только после успешного разбора всей её выдачи**, и не назад:
   берётся максимум из «нашего сейчас» и времени последней полученной записи (если часы
   устройства отстают от серверных, выдача не зациклится на уже полученных строках).
   Упавшая таблица сохраняет свой курсор → следующий `sync()` до-получает её изменения,
   остальные таблицы при этом идут своим курсом.

> ✅ Анти-эхо (`last_sync_id`, задача 3.6) сделано: колонка есть у всех синкаемых таблиц, сервер
> сам исключает из выдачи записи, изменённые этим же устройством.
>
> ✅ **Удаления доезжают (задача 3.9).** Сервер отдаёт удаления как tombstone: у таблиц с
> `deleted_at` (`clients`, `products`, `services`, `categories`, `equipment_models`, `orders`,
> `order_service`) — soft-deleted строка с `deleted: true`/`deleted_at`, у остальных — запись из
> `sync_tombstones` (`{ id, uuid_id, deleted: true, deleted_at }`). `syncService._syncServerToLocal`
> по такому признаку вызывает `_applyServerDeletion`: снимает «висящие» операции по записи
> (`operationsRepo.removeByLocalId`/`removeByServerId`) и удаляет строку локально — по `server_id`,
> а у связок без него (`order_service`) по клиентскому `uuid_id` (локально это `id`). Если удаляется
> заказ, каскадом убираются его строки (`order_service`, `order_product`, `materials`). Удалённая
> запись поэтому не «воскресает» следующей отправкой.
>
> ✅ **Владелец данных и авторизация (задача 3.10).** `/sync` и `/sync-updates` — под
> `auth:sanctum`: сервер знает пользователя, проставляет `user_id` при вставке и фильтрует выдачу
> по цепочке владельцев. Клиент пока не умеет логиниться — токен читается из `localStorage`
> (`auth_token`, задача 7.3/7.4) и подставляется в `Authorization: Bearer`; без токена сервер
> отвечает `401`, операции остаются в очереди и ничего не теряется.
>
> Известные ограничения LWW: правка и удаление одного и того же объекта с двух устройств
> разрешаются «по времени», а не по полям (без CRDT/version vectors, см. приложение PLAN.md).

> ✅ **Конфликты (задача 3.8).** Единицы времени сведены к одному стандарту: локально
> `created_at`/`updated_at` — целые UNIX-секунды, серверные ISO-строки приводятся хелпером
> `src/utils/timestamps.js` (`toEpochSeconds`). Оба роута синка (`/sync`, `/sync-updates`)
> отдают время в **ISO-8601 UTC** — «сырую» строку Postgres `Date.parse` принял бы за
> локальное время устройства, и версии «поехали» бы на часовой пояс.
> Правило разрешения конфликтов — **last-write-wins**: запись с более свежим `updated_at`
> побеждает, более старая версия не применяется; сравнение корректно и для старых локальных
> значений в миллисекундах.
> Сервер — источник версии: ответ `/sync` содержит `updated_at` по каждой операции, и
> `markSynced` применяет его к локальной записи (`insert` — по локальному UUID,
> `update`/`delete` — по `server_id`; значение переводится в секунды). Поэтому после своей
> отправки клиент и сервер согласованы по версии: своя же более старая копия запись не
> «воскрешает».
>
> Известные ограничения LWW: правка и удаление одного и того же объекта с двух устройств
> разрешаются «по времени», а не по полям (без CRDT/version vectors, см. приложение PLAN.md);
> удаления пока вообще не «доезжают» до второго устройства (3.9).
### 4.3. Устойчивость к сети и состояние синка (задача 3.7)

- ошибки отправки классифицируются (`_failureKind`): нет ответа сервера (сеть/таймаут) →
  `network`, HTTP 5xx → `server`, HTTP 4xx → `request`;
- для `network`/`server` ставится пауза до следующей попытки — backoff
  `5с → 15с → 60с → 5мин` (`syncService.status.nextRetryAt`); 4xx паузу **не** ставит,
  иначе один битый payload заблокирует всю очередь;
- `sync()` не ходит на сервер, если `navigator.onLine === false`, и пока действует пауза;
  ручной повтор из UI — `sync({ force: true })`;
- состояние для индикаторов (задачи 6.2/6.3): `syncService.getStatus()` и `subscribe()`
  отдают `{ online, syncing, lastError, consecutiveFailures, nextRetryAt, pendingCount }`;
- появление сети (`online`-событие) снимает паузу и, если автозапуск включён, **сразу**
  повторяет `sync({ force: true })` — отложенные офлайном операции не ждут таймера (6.3).

> ✅ Критерий 3.7: батч не уходит в цикл — при сбое операции возвращаются в `pending`,
> повтор возможен не раньше backoff, а `sync({ force: true })` остаётся для ручного повтора.

### 4.4. Что синхронизируется, а что нет

В `syncService.repos` и `fkTransformationMap` **включены**:
`specializations`, `categories`, `product_categories`, `equipment_models`, `clients`,
`services`, `products`, `orders`, `order_service`, а также (задача 3.4) `order_product`
(товары в заказе) и `materials` — **ручные позиции заказа** («купил на стороне»):
на сервере это таблица `materials` (`order_id, name, price, amount`), решение D2.
С задачи 9.2 добавлен **склад**: `incoming_products` (приходы) и `buy_product_prices`
(закупочные цены) — их пишет клиент приходом, — и `product_stocks` (остаток): его ведёт
**сервер** (`IncomingProductRepository::recordArrival()` увеличивает склад ровно один раз,
идемпотентность по `uuid_id`), а клиент только забирает значение выгрузкой и обновляет
локальную строку оптимистично.

С задачи 9.3 добавлена **история цен продажи**: `sales_products_prices` — запись создаётся там же,
где товар продаётся (добавление товара со склада в заказ), и убирается вместе со строкой заказа;
склад читает из неё «последнюю цену продажи». Там же закрыт дубль «где лежит товар»: в остатке
больше нет `product_categories_id` — категорию знает только `products.product_category_id`.

`incoming_products` / `product_stocks` / `buy_product_prices` / `sales_products_prices` устроены
так (кто источник истины, как не удвоить остаток) — см. `docs/DATA-MODEL.md` §«Склад: приход,
остаток, закупочная цена» и задачи 9.2/9.3.

✅ `/api/arrival_product` закрыт (задача 11.7, бывший O-6): под `auth:sanctum`
(`EnsureFrontendRequestsAreStateful` — web-версия входит по сессии и шлёт `X-CSRF-TOKEN`,
bearer-токен тоже принимается). Без токена/сессии — `401`, чужой товар — `403`
`FORBIDDEN_NOT_OWNER` (та же цепочка владельца, что в синке).

✅ `order_service` на сервере — связка **без PK**: `server_id` у неё нет, поэтому идентичность
строки на всех устройствах — **клиентский UUID** (`id` локально ↔ `uuid_id` на сервере,
задача 3.5). `insert` дедуплицируется по `order_id + service_id`, `delete` уходит по этому же
натуральному ключу (серверные id), `applyServerRecord` матчит строку по `uuid_id` (а не по
`server_id`). Правка заказа больше не оставляет дублей работ на сервере.

### 4.5. Платформенный слой: настоящий SQLite на Android (Фаза 4)

- **Один интерфейс — два адаптера.** Репозитории и `syncService` работают через
  `src/database/db.js` (делегат на активный адаптер), иначе выбор в boot не имел бы смысла:
  на Android продолжал бы работать sql.js из памяти. Выбирает адаптер `src/boot/db.js`
  по `Capacitor.isNativePlatform()` (см. `src/utils/platform.js`).
- **Android** → `@capacitor-community/sqlite` (линия 7.x): соединение создаётся через
  `SQLiteConnection` (`checkConnectionsConsistency` → `retrieveConnection`/`createConnection`
  → `open()`), DDL/PRAGMA идут батчем через `execute`, параметризованные INSERT/UPDATE — через
  `executeSet`, SELECT — через `query`, транзакции — плагинными `begin/commit/rollback`.
  Внутри своей транзакции адаптер передаёт плагину `transaction: false`, иначе получился бы
  вложенный `BEGIN` (SQLite так не умеет). БД — файл
  `data/data/<package>/databases/ledgercraftSQLite.db`, шифрование пока выключено
  (SQLCipher требует хранения пароля — задача Фазы 7).
- **Браузер** → sql.js + дамп в localStorage/IndexedDB (Фаза 1).
- **Версия схемы (4.5).** Эталон — `SCHEMA_VERSION` = число миграций
  (`src/database/schema-version.js`). В плагине 7.x нет `setVersion`, поэтому версия схемы —
  это `PRAGMA user_version` в самом файле БД (её же читает нативный `getVersion()`);
  `src/database/migrate.js` после прогона миграций сверяет её и число применённых миграций
  с эталоном.
- **Бэкап (4.4).** На Android — `exportToJson('full')` + `@capacitor/filesystem` (документы
  устройства, при недоступности — приватная папка приложения), автоматически раз в сутки при
  старте; в браузере — скачивание дампа `.sqlite`. Кнопка — в `OthersPage.vue`.
- **Совместимость нативного SQLite.** Плагин использует системный SQLite (minSdk 23 →
  Android 6 = SQLite 3.8), поэтому синтаксис, требующий SQLite ≥ 3.24, в локальных запросах
  запрещён: найденный UPSERT (`ON CONFLICT … DO UPDATE`) в `metaRepo` заменён на
  `INSERT OR REPLACE` (задача 4.x-примечание в TODO).
- **Бэкап и восстановление (4.4/11.9).** `backupService` умеет и создавать, и восстанавливать
  копию. Обычный перенос на новый телефон — это **вход + синк** (все синкаемые таблицы тянутся
  выгрузкой `since=0`); восстановление из файла — **аварийный путь без сервера** (сервер потерян
  или недоступен, аккаунт удалён). Восстановление доступно только на устройстве (там бэкап — JSON),
  сверяет `SCHEMA_VERSION` до импорта и требует подтверждения — локальная БД заменяется целиком.
- **Что ещё не сделано:** нативная сборка (`src-capacitor` добавлен, но платформа Android не
  сгенерирована — нужен JDK + Android SDK: `npx cap add android`), проверка на живом
  устройстве (11.8), включая восстановление из бэкапа (11.9).

### 4.6. Фоновый запуск и индикатор синка (Фаза 6)

- **Неблокирующий старт (6.1).** `boot/db.js` теперь только готовит локальную БД; синк
  запускает `src/App.vue` в `onMounted()` через `syncService.startAutoSync()`. Первый проход
  планируется `setTimeout(…, 0)` — интерфейс рисуется раньше сетевых запросов, — а далее
  повторяется по таймеру (`AUTO_SYNC_INTERVAL_MS`, по умолчанию 60 с). Автозапуск
  идемпотентен, `stopAutoSync()` снимает и таймер, и ещё не начатый первый проход.
- **Автоповтор при выходе из офлайна (6.3).** `window.addEventListener('online')` →
  `_handleOnline()`: состояние переводится в «сеть есть», снимается пауза backoff и (если
  автозапуск включён) немедленно выполняется `sync({ force: true })`. `offline` — состояние
  «нет сети»; операции при этом просто копятся в очереди и не теряются.
- **Индикатор (6.2).** `src/components/SyncStatusBar.vue` — компактный чип в шапке
  `MainLayout.vue` на основных экранах; вне каркаса (страница заказа, вход, 404) `App.vue`
  монтирует его с `floating`, и чип крепится внизу слева, как раньше. Он подписан на
  `syncService.subscribe()`, на старте зовёт `refreshStatus()` (размер очереди живёт в БД),
  а тап по бейджу — ручной `sync({ force: true })`. Приоритет состояний и текст вынесены в
  чистую функцию `src/utils/syncStatusView.js` (`нет сети` → `синхронизация…` →
  `ошибка синка` → `не отправлено: N` → `синхронизировано`), поэтому она проверяется обычным
  юнит-тестом, а компонент остаётся «тонким».

## 5. UI и состояние

- Страницы напрямую не ходят в БД; они используют Pinia-сторы.
- Сторы делают «оптимистичное» обновление: сначала меняют `items`, потом зовут репозиторий,
  при ошибке откатывают состояние.
- Репозитории вызываются **только** из сторов: после Фазы 8 в `*.vue` нет ни одного импорта
  `*Repo` (критерий 8.2). Страница заказа держит данные в `useOrderDraftStore`
  (черновик заказа + справочники), а её форма разбита на `src/components/order/*`
  (шапка, селекторы клиента/модели, панели, списки, редакторы, итоги, диалоги) — 8.1.
- Позиционные аргументы SQL собирают именованные мапперы `src/database/mappers/*`
  (заказы, строки заказа, клиенты/работы/модели) — 8.3. Раньше `ordersRepo` собирал массив
  из 16 значений «на глаз».

## 6. Роли файлов (кратко)

| Путь | Роль |
|---|---|
| `src/boot/*` | инициализация при старте (axios, БД + миграции, pinia); синк — в `App.vue` (6.1) |
| `src/App.vue` | корень приложения: запуск фонового синка и индикатор состояния (6.1/6.2) |
| `src/components/SyncStatusBar.vue` | индикатор сети и синка (6.2): офлайн / синк / ошибка / очередь + ручной повтор |
| `src/utils/syncStatusView.js` | чистое представление состояния синка для индикатора (6.2) |
| `src/services/api.js` | axios-клиент к серверу (единственное место сетевых вызовов) |
| `src/services/syncService.js` | движок синхронизации |
| `src/services/backupService.js` | бэкап локальной БД (нативный — файл, веб — дамп) |
| `src/components/order/*` | компоненты страницы заказа (8.1): шапка, селекторы клиента/модели, панели «все»/«работы»/«материалы», списки и редакторы позиций, итоги, 5 диалогов |
| `src/stores/useOrderDraftStore.js` | черновик заказа: позиции, справочники формы, сохранение (единственное место, где страница заказа ходит в репозитории — 8.1/8.2) |
| `src/repositories/*` | работа с локальной БД + постановка операций в очередь |
| `src/database/mappers/*` | именованные мапперы позиционных SQL-аргументов (8.3) |
| `src/repositories/operationsRepo.js` | очередь `operations` (статусы `pending`/`sending`/`synced`/`failed`, `attempts`, восстановление in-flight операций, очистка «сдавшихся») |
| `src/repositories/metaRepo.js` | метаданные (`last_synced_at:<table>`, отметка о бэкапе) |
| `src/utils/logger.js` | DEV-логи + кольцевой буфер для «Режима разработчика» (12.5) |
| `src/utils/errorLog.js` | **постоянный** буфер ошибок (Фаза 14): `warn`/`error`, переживает перезапуск, пишется и на боевой сборке |
| `src/utils/feedbackView.js` | чистая сборка отчёта «Сообщить об ошибке» (контракт `docs/FEEDBACK.md` §3) |
| `src/repositories/feedbackRepo.js` | локальная очередь отчётов (Фаза 14): `pending`/`sending`/`sent`/`failed` |
| `src/services/feedbackService.js` | отправка отчётов: `submit()` офлайн-первый, `flush()` рядом с синком |
| `scripts/pull-feedback.mjs` | `npm run feedback:pull` — выгрузка отчётов в `feedback/INBOX.md` (то, что читает агент) |
| `src/database/db.js` | единая точка доступа к БД (делегат на активный адаптер) |
| `src/database/migrate.js` | прогон миграций + сверка версии схемы с эталоном |
| `src/database/schema-version.js` | `SCHEMA_VERSION` — эталон схемы (число миграций) |
| `src/database/adapters/*` | адаптеры БД: sql.js (браузер) и нативный SQLite (Android) |
| `src/database/migrations/*` | версии схемы локальной БД |
| `src/database/queries/*` | SQL-запросы для репозиториев |
| `src/stores/*` | Pinia-сторы (состояние UI-сущностей) |
| `src/pages/*` | страницы; `src/pages/dialogs/*` — модальные диалоги |
| `src-capacitor/*` | Capacitor-проект (Android): `capacitor.config.json`, зависимости плагинов |

## 7. Главные архитектурные проблемы

1. ~~**БД не сохраняется на диск**~~ — исправлено в Фазе 1 (дамп sql.js в
   localStorage/IndexedDB) и в Фазе 4 (на Android — настоящий файл SQLite на диске).
2. ~~**sql.js грузится с CDN**~~ — исправлено в Фазе 1 (`public/sql-wasm.wasm`).
3. **Sync-движок** отправляет операции волнами с топологической сортировкой по FK, статусами
   очереди и устойчивостью к сети (задачи 3.1–3.4, 3.7), курсор выдачи — свой у каждой таблицы
   (3.6, FE-часть), нет координации одновременных `sync()` из разных вкладок (флаг `syncing`
   живёт только в памяти). Повторная отправка «подозрительной» операции дублей не создаёт —
   сервер идемпотентен по `uuid_id` (задача 3.5).
4. **Платформенный слой собран (Фаза 4):** нативный адаптер подключён, зависимости на месте,
   `src-capacitor` создан; версия схемы на устройстве сверяется с эталоном, бэкап есть.
   Осталось: сгенерировать платформу Android (`npx cap add android`, нужен JDK + Android SDK),
   проверить на живом устройстве, включить шифрование файла БД (Фаза 7).
5. **Синхронизация связных таблиц** (`order_product`, `materials`) сделана в 3.4; не подключены
   к офлайн-слою `incoming_products`, `product_stocks`, `buy_product_prices`,
   `sales_products_prices` (задачи 9.2/9.3).
6. **Тесты есть (Фаза 5, дополнены Фазой 10):** `npm test` → vitest, **201 тест** (миграции,
   репозитории, синк, автосинк, индикатор, storage, DEV-моки, вход/PIN, auth-guard, стор заказа,
   лексикон/пресеты/флаги/guard, поля профиля); на сервере — PHPUnit
   (`php artisan test` → `72 passed`, exit 0).

## 8. Мульти-специализация (рабочие профили) — реализовано (Фаза 10)

Специализация — это **рабочий профиль мастерской**, и она уже является сквозной осью архитектуры:
на сервере `User hasMany Specialization` (`specializations.user_id`), а `categories`,
`product_categories`, `equipment_models`, `clients`, `orders` держатся за `specialization_id`; синк
фильтрует выдачу по цепочке владельцев (задача 3.10). Поэтому один аккаунт может вести несколько
специализаций («веломастер + аквариумист») **без изменения схемы** — активный профиль в UI это
`useSpecializationsStore.selectedId`.

Фаза 10 добавила к этой оси продуктовый слой: пресеты (стартовый каталог + метаданные UI),
лексикон терминов, акцентный цвет (`setCssVar`) и онбординг/регистрацию. Решения **D4**/**D5**,
задачи **10.1–10.9** (`docs/PLAN.md`, `TODO.md`). Осознанно **не делаем** schema-per-ниша и
отдельные формы заказа под специализацию — это ломает офлайн-модель и синк; схема заказа остаётся
универсальной (добавлено одно опциональное поле `equipment_identifier` — VIN / серийник рамы /
адрес объекта). Данные — `docs/DATA-MODEL.md` («Рабочие профили…»), UI — `docs/FRONTEND.md` §9.

## 9. Аналитика: одна методика выручки (Фаза 9, задача 9.1)
Аналитика считается по **локальной** БД (`analyticsRepo` + `utils/analytics.js`), а не запросом к
серверу: приложение офлайн-первое, страница должна открываться без сети. Чтобы цифры на клиенте и в
серверных отчётах совпадали, методика зафиксирована ровно одна — и на сервере
(`StatisticRepository::billedOrdersSubquery()`), и на клиенте:

| Правило | Значение |
|---|---|
| «Учтённый» заказ | `status = 'done'` **и** `paid` **и** `deleted_at IS NULL` |
| Выручка заказа | позиции: работы (`order_service.quantity × sale_price`) + товары (`order_product.quantity × sale_price`) + ручные материалы (`materials.amount × price`) |
| Себестоимость заказа (9.5/9.6) | закупка позиций: `order_product.quantity × buy_price` + `materials.amount × buy_price`; у работ себестоимости нет (труд мастера) |
| Маржа / наценка | маржа = выручка − себестоимость; наценка = маржа ÷ себестоимость × 100 (`null`, если закупок в периоде нет) |
| `buy_price IS NULL` | «закупка неизвестна»: строка считается с нулевой себестоимостью, маржа частичная, наценка не показывается |
| Цена позиции | `sale_price` позиции важнее каталожной `services.price` (в заказе могла быть скидка) |
| Дата периода | `orders.updated_at` — дата закрытия/последнего изменения заказа (как в исходных серверных отчётах) |
| Средний чек | выручка ÷ число учтённых заказов (заказ без позиций входит нулевым) |
| Распределение по статусам | текущие статусы **всех** не удалённых заказов мастерской (без периода) |

Границы масштабов одинаковы с обеих сторон: день — 30 дней, неделя — 15 недель (ISO-неделя,
понедельник), месяц — 12 месяцев, год — 5 лет; `orders.total_amount` в расчётах **не участвует**
(это сохранённая сумма, а не источник истины).

Клиент: `pages/AnalyticPage.vue` → `stores/useAnalyticsStore.js` → `repositories/analyticsRepo.js` →
`database/queries/analytics.js`, правила — `utils/analytics.js`. Сервер: `StatisticRepository` +
`StatisticController` (плюс ручки `get_top_products`, `get_top_materials`, `get_orders_status`).
Тесты: `test/analytics.test.js` и `test/analytics-repo.test.js` (клиент),
`LedgerCraftDocker03/tests/Feature/StatisticRepositoryTest.php` (сервер) — контрольная цифра набора
одна и та же (1700 ₽, 2 заказа, средний чек 850 ₽).

## 10. Обратная связь и отчёты об ошибках (Фаза 14) — реализовано (14.1–14.7)

Задачи **14.1–14.10**, решение **D7**, контракт — `docs/FEEDBACK.md`. Смысл: мастер работает на
телефоне, где нет консоли, а отчёт должен доехать до разработчика **и ИИ-агента** — поэтому
последнее звено цепочки это **файл в репозитории** (`feedback/INBOX.md`), а не БД и не мессенджер.
Рабочая петля сделана: постоянный буфер ошибок → очередь отчётов → `POST /api/feedback` →
`npm run feedback:pull` → инбокс. Осталось 14.8–14.10 (авто-отчёт о сбое, правило разбора в
`feedback/DECISIONS.md`, живой прогон на dev-VPS).

Три слоя, каждый по образцу уже существующего:

| Слой | Что добавляется | На что похоже |
|---|---|---|
| Устройство | постоянный буфер ошибок `utils/errorLog.js` + глобальные перехватчики (`boot/errorLog.js`), очередь `feedback_reports` (`feedbackRepo`, миграция 030) | буфер логов 12.5, очередь операций синка `operations` (3.3) |
| Сервер | `POST /api/feedback` (идемпотентность по `uuid_id`), `GET /api/feedback` под pull-токеном, `php artisan feedback:export` | идемпотентность и владелец из 3.5/3.10 |
| Доставка агенту | `npm run feedback:pull` → `feedback/INBOX.md` + `feedback/inbox/*.md`; разбор — `feedback/DECISIONS.md` | правило «ни одного отчёта в воздухе» (как чек-лист 11.5) |

Границы: отчёты **не** проходят через `/api/sync` (не таблица `$tables`, не «полный сброс», не бэкап);
в payload нет данных мастерской (заказы/клиенты/суммы/телефоны); отправка — только по действию
пользователя. Сырые отчёты и дайджест — в `.gitignore`, в git живут `feedback/README.md` и
`feedback/DECISIONS.md`.

Подробности, карта файлов и известные баги — в `docs/FRONTEND.md` и `docs/DATA-MODEL.md`.