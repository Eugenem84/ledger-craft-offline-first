<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Тема, которую реально ставит приложение: `BridgeActivity.onCreate()` вызывает
         `setTheme(R.style.AppTheme_NoActionBar)` ДО `setContentView()` (см. capacitor-android).
         Приложение всегда тёмное (Quasar `dark: true`), поэтому системные панели — чёрные
         со светлыми иконками. На Android 15+ (`targetSdk 35`) edge-to-edge включён
         принудительно и `android:statusBarColor` не применяется: в полосах виден фон
         окна (`android:windowBackground`), поэтому он тоже чёрный. Отступы под системные
         панели задаёт Capacitor (`android.adjustMarginsForEdgeToEdge: "auto"` в
         `src-capacitor/capacitor.config.json`) — CSS `env(safe-area-inset-*)` на Android
         для статус-бара не работает. -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>

        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
    </style>
</resources>