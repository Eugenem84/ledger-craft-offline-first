# `DECISIONS.md` — журнал разбора отчётов (Фаза 14, задача 14.9)

Заполняется после каждой выгрузки (`npm run feedback:pull`): у каждого отчёта должен быть вердикт —
баг это или нет, и куда он ушёл. Персональные данные сюда **не** переносим (отчёты лежат в
`feedback/inbox/`, он в `.gitignore`); здесь только дата, короткая суть и решение.

| Дата       | Отчёт (`feedback/inbox/…`)                    | Вердикт                                                                                                                            | Куда ушло                                                                                                                                        |
| ---------- | --------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| 13.09.2026 | `2026-09-13_23-35-38-cf4bffe3.md` (сервер #1) | **баг подтверждён:** плавающий чип синка на карточке заказа перекрывал список позиций (маршрут вне `MainLayout` — чип висел в 72px от низа, таббара там нет) | задача **14.11**, исправлено: чип живёт только в шапке `MainLayout`, плавающий вариант удалён; регресс — `test/pages-layout.test.js`; релиз 1.2 → 1.3 |
| 14.09.2026 | `2026-09-14_09-40-09-420674ad.md` (сервер #2) | **баг подтверждён и воспроизведён локально:** удаление заказа падало `FOREIGN KEY constraint failed` — на Android БД открыта с `PRAGMA foreign_keys = ON`, а UI-путь сносил только строку `orders`, не трогая `order_service`/`materials`; у синканого заказа tombstone позже «спасал» его (отсюда «то удаляется, то нет»), у offline-заказа — «залипал» навсегда. Из хвоста отчёта подтвердились ещё два дефекта: та же ошибка при удалении услуги/клиента, занятых в заказе, и `bad base-64` в автобэкапе (бэкап на Android не создавался вообще) | задача **14.15**, исправлено: `ordersRepo.remove` чистит строки заказа в транзакции (как `_applyServerDeletion`), в тестах включены внешние ключи, ошибки экрана пишутся в `errorLog`; отсюда же — **14.16** (услуга/клиент в заказах) и **14.17** (автобэкап/logExport: нет `encoding`); при проверке фикса нашёлся ещё и дефект «полного сброса» на внешних ключах — **14.18** |
| 15.09.2026 | `2026-09-15_08-38-18-31908428.md` (сервер #3) | **предложение принято** (после уточнения владельца): «указывать количество работ» — это не дубли строк, а **количество в строке**: «подкачать колесо» делают 2–4 раза, и так с любой услугой; уточнено владельцем: то же нужно для товаров со склада. Из хвоста отчёта: `bad base-64` — уже открытая 14.17, «`order_service/insert` отложена до следующей волны» — нормальный ход волн синка | задача **14.19**, сделано: количество у работ и товаров (поле «кол-во» у уже выбранной работы в каталоге, колонка «кол-во» в списке работ, поле «Количество» в диалоге товара), целое ≥ 1 через `src/utils/quantity.js`, меняется только в режиме правки; сервер и схема не менялись (колонка и приём `quantity` были). Владелец проверил в браузере и принял; релиз **1.6 (versionCode 7)** |
| 15.09.2026 | `2026-09-15_12-43-06-c49749a4.md` (сервер #4) | **баг подтверждён — и это был корень всех бед OTA:** в хвосте отчёта `"LiveUpdate.then()" is not implemented on android` + `[Update] OTA: плагин не ответил на ready(): импорт плагина OTA: нет ответа за 3000 мс`. Прокси плагина Capacitor на незнакомое свойство отвечает ошибкой, а `then` — ровно такое свойство; промис, который «оседает» на самом прокси, из-за этого **не завершается никогда** (движок вызывает `then` как thenable, прокси оставляет промис в ожидании). Мост к плагину OTA отдавал прокси наружу → молчали `ready()`, текущий бандл и версия из плагина. Второй след отчёта — `Cannot read properties of undefined (reading 'versionName')` — тот же релиз, но другая причина: стор подменял состояние объектом версии (задача 15.13) | задачи **15.13** (сборка 1.12) и **15.14** (сборка 1.13): стор больше не подменяет `status`, мост отдаёт обычную обёртку с привязанными методами (как `loadApkInstaller()` → `{ ApkInstaller }`), импорт плагина под таймаутом и мемоизирован. Регрессии: `test/update-store.test.js` (4) и `test/live-update-bridge.test.js` (3 — плагин подменён «строгим» прокси). Улики: **450 тестов**, линт 0, APK **1.13 (code 14)** + бандл `1.13.260915-1245` на dev |
| 15.09.2026 | `2026-09-15_12-54-35-64c94862.md` (сервер #5) | **баг подтверждён:** чип «доступно обновление 1.13» появился (фикс 15.14 сработал), но по кнопке «Обновить» — `Checksum mismatch`. Причина: плагин OTA считает sha256 **скачанного zip** и приводит его к **hex** (`getChecksumForFileAsString`), а сервер публиковал sha256 в **base64** — так написано в документации плагина, но для self-hosted zip это неверно | задача **15.15** (сборка приложения не менялась, только бэкенд): в манифест уходит `checksum` = hex, рядом `checksumBase64` справочно, `--checksum` принимает оба формата; бандл `1.13.260915-1245` перезаписан с hex `5649d08f…`; BE-тесты `BundleReleaseTest` — 14 |

Формат вердикта: **баг** → ID задачи в `TODO.md` (с приоритетом и критерием «Готово»);
**не баг / предложение** → объяснение, почему не берём или куда отложили (по возможности ID задачи).
